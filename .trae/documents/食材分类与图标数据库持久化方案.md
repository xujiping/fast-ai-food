用户希望将食材的分类和图标信息持久化到数据库，并在“食材库”页面进行分类展示，同时简化首页展示。

### 1. 数据库变更 (SQL)

我将创建一个 SQL 迁移文件，您需要在 Supabase 后台执行它。

* **变更内容**：给 `ingredients` 表添加 `category` (分类) 和 `icon` (图标) 两个字段。

### 2. 后端 API 升级 (`api/routes/ingredients.ts`)

* **写入逻辑**：在添加食材 (`POST`) 时，支持接收前端传递的分类和图标信息。

* **读取逻辑**：确保 `GET` 接口返回这些新字段。

### 3. 前端状态管理升级 (`src/store/useRecipeStore.ts`)

* **智能匹配**：在添加食材时（无论是手动输入还是快捷选择），利用前端预设的 `PRESET_INGREDIENTS` 数据自动匹配对应的分类和图标，并发送给后端。

* **类型定义**：更新 `PantryItem` 接口以包含新字段。

### 4. 页面改造

* **食材库页面 (`src/pages/Ingredients.tsx`)**：

  * **核心重构**：从简单的列表改为**分类视图**。

  * **功能增强**：展示图标，支持按分类浏览用户的食材库存。

* **首页 (`src/pages/Home.tsx`)**：

  * **简化展示**：移除之前的分类选择器组件。

  * **回归简洁**：恢复为“我的常买”快捷列表，但升级为**带图标**的胶囊按钮，既美观又不占空间。

### 实施步骤

1. 创建 SQL 迁移文件。
2. 更新后端 API 处理逻辑。
3. 更新前端 Store 的数据处理逻辑。
4. 重构食材库页面 (`Ingredients.tsx`)。
5. 简化首页 (`Home.tsx`)。

